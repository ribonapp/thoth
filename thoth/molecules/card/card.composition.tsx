import React from "react";
import { Card } from "./card";

export const BasicCard = () => (
  <Card
    text="hello from Card"
    buttonText="hello from ribon Button"
    buttonType="textOnly"
    buttonColor="ribon"
    buttonSize="large"
  />
);
